<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Formação</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <h1>🎓 Formação</h1>
    <a href="/" class="btn">🏠 Voltar</a>

    <h2>Adicionar Formação</h2>
    <form action="/formacao" method="POST">
      <input type="text" name="curso" placeholder="Curso" required>
      <input type="text" name="instituicao" placeholder="Instituição" required>
      <input type="text" name="periodo" placeholder="Período (ex: 2024 - 2026)" required>
      <button type="submit">Adicionar</button>
    </form>

    <h2>Formações Cadastradas</h2>
    <% if (formacoes.length === 0) { %>
      <p>Nenhuma formação cadastrada.</p>
    <% } else { %>
      <% formacoes.forEach(f => { %>
        <div class="card">
          <h3><%= f.curso %></h3>
          <p><%= f.instituicao %> — <%= f.periodo %></p>

          <form action="/formacao/<%= f.id %>?_method=PUT" method="POST">
            <input type="text" name="curso" value="<%= f.curso %>">
            <input type="text" name="instituicao" value="<%= f.instituicao %>">
            <input type="text" name="periodo" value="<%= f.periodo %>">
            <button type="submit">✏️ Atualizar</button>
          </form>

          <form action="/formacao/<%= f.id %>?_method=DELETE" method="POST">
            <button type="submit">🗑️ Excluir</button>
          </form>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>
